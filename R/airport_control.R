
#' @export
airport_control <- function(
  flight_level = AirportSim::airport_list_1$flight_level,
  registration = 0,
  flight_effect = FALSE,
  threshold = 6,
  var = diag(2),
  method = "DP",
  breaks
){

  return(as.list(environment(), all=TRUE))

}
